# ---- Secrets / env (NEVER commit these) ----
.env
.env.*
*.local
# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
*-service-account*.json
google-services.json
GoogleService-Info.plist
.firebaserc.local

# ---- Node / JS (React Native, web, Cloud Functions) ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
build/
coverage/

# ---- Expo / React Native ----
.expo/
.expo-shared/
*.jks
*.p8
*.p12
*.keystore

# ---- Flutter / Dart (in case we go this way) ----
.dart_tool/
.packages
.pub-cache/
.pub/
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec

# ---- OS / editor junk ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Firebase service-account keys (secret — never commit)
serviceAccount.json
**/serviceAccount.json

# OFF seeder resume state (local only)
.off-seed-progress.json
